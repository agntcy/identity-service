// Copyright 2025 Copyright AGNTCY Contributors (https://github.com/agntcy)
// SPDX-License-Identifier: Apache-2.0

// This file was autogenerated by go-to-protobuf. Do not edit it manually!

syntax = "proto3";

package agntcy.identity.platform.v1alpha1;

// Package-wide variables from generator "generated".
option go_package = "github.com/agntcy/identity-platform/api/server/agntcy/identity/platform/v1alpha1;identity_platform_sdk_go";

// An Identity API Key.
message ApiKey {
  optional string api_key = 1;
}

// Duo IdP Settings
message DuoIdpSettings {
  optional string hostname = 1;

  optional string integration_key = 2;

  optional string secret_key = 3;
}

// Issuer Settings
message IssuerSettings {
  // A unique identifier for the Issuer.
  // This is typically the Issuer's ID in the Identity.
  optional string issuer_id = 1;

  // A unique identifier for the Key.
  // This is typically the Key's ID in the vault provider.
  optional string key_id = 2;

  // The type of the IdP.
  optional IdpType idp_type = 3;

  // Settings for the Duo Identity Provider.
  optional DuoIdpSettings duo_idp_settings = 4;

  // Settings for the Okta Identity Provider.
  optional OktaIdpSettings okta_idp_settings = 5;
}

// Okta IdP Settings
message OktaIdpSettings {
  optional string org_url = 1;

  optional string client_id = 2;

  optional string private_key = 3;
}

// Devices used for user approval
message Device {
  // A unique identifier for the Device.
  optional string id = 1;

  // User ID associated with the Device.
  optional string user_id = 2;

  // Subscription Token for the Device.
  optional string subscription_token = 3;
}

// Identity Settings
message Settings {
  // An API Key for the Identity Platform.
  optional ApiKey api_key = 1;

  // Settings for the Issuer.
  optional IssuerSettings issuer_settings = 2;

  // Devices registered for user approval.
  repeated Device devices = 3;
}

// Type
enum IdpType {
  // Unspecified Envelope Type.
  IDP_TYPE_UNSPECIFIED = 0;
  // Idp Type Duo.
  IDP_TYPE_DUO = 1;
  // Idp Type Okta.
  IDP_TYPE_OKTA = 2;
  // Idp Type Self.
  IDP_TYPE_SELF = 3;
}

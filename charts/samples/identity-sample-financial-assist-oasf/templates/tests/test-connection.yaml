# Copyright 2025 Copyright AGNTCY Contributors (https://github.com/agntcy)
# SPDX-License-Identifier: Apache-2.0

apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "identity-sample-financial-assist-oasf.fullname" . }}-test-connection"
  labels:
    {{- include "identity-sample-financial-assist-oasf.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "identity-sample-financial-assist-oasf.fullname" . }}:{{ .Values.platform.port }}']
  restartPolicy: Never

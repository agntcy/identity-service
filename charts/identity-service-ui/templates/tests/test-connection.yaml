# Copyright 2025 AGNTCY Contributors (https://github.com/agntcy)
# SPDX-License-Identifier: Apache-2.0

apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "identity-service-ui.fullname" . }}-test-connection"
  labels:
    {{- include "identity-service-ui.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "identity-service-ui.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never

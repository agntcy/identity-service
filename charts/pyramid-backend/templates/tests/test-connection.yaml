# Copyright 2025 Copyright AGNTCY Contributors (https://github.com/agntcy)
# SPDX-License-Identifier: Apache-2.0

apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "pyramid-backend.fullname" . }}-test-connection"
  labels:
    {{- include "pyramid-backend.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "pyramid-backend.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never

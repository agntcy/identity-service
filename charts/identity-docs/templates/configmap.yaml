# Copyright 2025 Copyright AGNTCY Contributors (https://github.com/agntcy)
# SPDX-License-Identifier: Apache-2.0

apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ $.Release.Name }}-configmap-docs
data:
  {{- range $key, $value := $.Values.configmap }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
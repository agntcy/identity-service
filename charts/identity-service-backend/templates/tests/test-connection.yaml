# Copyright 2025 Cisco Systems, Inc. and its affiliates
# SPDX-License-Identifier: Apache-2.0

apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "identity-service-backend.fullname" . }}-test-connection"
  labels:
    {{- include "identity-service-backend.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "identity-service-backend.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
